import React from 'react';
import TextField from '@material-ui/core/TextField';

const Filters = () => (
    <div>
        <TextField
          id="name"
          label="Name"
          margin="normal"
        />
    </div>
);

export default Filters;